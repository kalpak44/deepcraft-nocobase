[defaults]
inventory         = inventory.yml
host_key_checking = False
# Not `stdout_callback = yaml`: that community.general plugin was removed in
# version 12.0.0, superseded by this option on the built-in default callback.
result_format     = yaml

[ssh_connection]
retries = 5

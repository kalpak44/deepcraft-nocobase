{{ '#' }} Managed by ansible/roles/lex_mcp — edits on the host are overwritten.
{{ '#' }}
{{ '#' }} Kept out of the unit file, which is world-readable: an EnvironmentFile can be
{{ '#' }} 0600 and readable only by the account the service runs as. Same reasoning as
{{ '#' }} ciela-mcp's credentials file.
{% if lex_mcp_takeover_token | length > 0 %}
BROWSER_TAKEOVER_TOKEN={{ lex_mcp_takeover_token }}
{% else %}
{{ '#' }} BROWSER_TAKEOVER_USER / BROWSER_TAKEOVER_PASSWORD are not set, so Lexy hands
{{ '#' }} out the plain takeover URL and whoever opens it signs in themselves.
{% endif %}